# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Virtual environments
venv/
.venv/
env/

# Common ignores
node_modules
.next/
.DS_Store

# Runtime hospital data — this is live application data, not source code.
# It must never go through a git merge/rebase: a conflict here previously
# got committed with literal <<<<<<< markers, which corrupted the JSON and
# caused every account to be silently wiped on the next read. Back it up
# via data/backups/ (kept locally, see lib/doctor-store.ts) instead.
data/doctor-store.json
data/backups/